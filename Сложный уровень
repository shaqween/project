"""Исполнитель Папус преобразует число на экране.
 У исполнителя есть две команды, которым присвоены номера:
1. Прибавить 1
2. Увеличить каждый разряд числа на 1
Сколько существует программ,
для которых при исходном числе 18 результатом является число 74?
(Для второй команды: например, число 12 превратится в 23, число 19 в 29, то есть,
если перед выполнение программы какая-то цифра равна 9, то она не меняется)"""
def f(curr, end):
    if curr == end: return 1
    if curr>end: return 0
    if curr<end and curr%10 != 9:
        return f(curr+1, end) + f(curr+11, end)
    else:
        return f(curr+1, end) + f(curr+10, end)
print(f(18, 74))



